# Cloudflare Containers 하이브리드 아키텍처

## 🏗️ **아키텍처 개요**

### **현재 문제점:**
- Cloudflare Access로 인한 API 접근 제한
- Workers의 제한된 실행 시간 (10초)
- 복잡한 비즈니스 로직 처리 어려움
- 파일 업로드/처리 제약

### **하이브리드 솔루션:**

```
┌─────────────────────────────────────────────────────────────┐
│                    Cloudflare Edge Network                  │
├─────────────────────────────────────────────────────────────┤
│  🌐 Cloudflare Pages (Frontend)                            │
│  ├── React App (Static)                                    │
│  └── CDN + Global Distribution                             │
├─────────────────────────────────────────────────────────────┤
│  ⚡ Cloudflare Workers (API Gateway)                       │
│  ├── Authentication & Authorization                        │
│  ├── Request Routing & Load Balancing                      │
│  ├── CORS & Security Headers                               │
│  └── Simple Business Logic                                 │
├─────────────────────────────────────────────────────────────┤
│  🐳 Cloudflare Containers (Backend Services)              │
│  ├── Spring Boot Application                               │
│  ├── Complex Business Logic                                │
│  ├── File Processing                                       │
│  ├── AI/ML Operations                                      │
│  └── Database Operations                                   │
├─────────────────────────────────────────────────────────────┤
│  🗄️ Cloudflare D1 (Database)                              │
│  ├── User Data                                             │
│  ├── Reading Plans                                         │
│  └── Statistics                                            │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 **역할 분담**

### **Cloudflare Workers (API Gateway)**
- ✅ **인증/인가**: JWT 토큰 검증
- ✅ **라우팅**: 요청을 적절한 Container로 전달
- ✅ **CORS**: Cross-Origin 요청 처리
- ✅ **캐싱**: 자주 사용되는 데이터 캐싱
- ✅ **보안**: Rate Limiting, DDoS 보호

### **Cloudflare Containers (Backend Services)**
- ✅ **비즈니스 로직**: 복잡한 계산 및 처리
- ✅ **파일 처리**: 이미지 업로드, 문서 생성
- ✅ **AI 통합**: OpenAI API 연동
- ✅ **데이터베이스**: 복잡한 쿼리 및 트랜잭션
- ✅ **외부 API**: 성경 API, 기타 서비스 연동

## 🚀 **구현 계획**

### **Phase 1: Container 설정**
1. **Spring Boot 애플리케이션 컨테이너화**
2. **Cloudflare Containers 배포**
3. **Workers와 Container 연동**

### **Phase 2: 서비스 분리**
1. **API Gateway (Workers)**
2. **Core Services (Containers)**
3. **Database Layer (D1)**

### **Phase 3: 최적화**
1. **캐싱 전략**
2. **로드 밸런싱**
3. **모니터링**

## 💰 **비용 분석**

### **Cloudflare Workers**
- **무료**: 100,000 요청/일
- **Pro**: $5/월 (10M 요청)

### **Cloudflare Containers**
- **무료**: 100,000 요청/일
- **Pro**: $5/월 (10M 요청)
- **CPU 시간**: $0.50/GB-초

### **예상 월 비용**
- **Free Tier**: $0-10/월
- **Pro Tier**: $10-20/월

## 🔧 **기술 스택**

### **Frontend**
- React 18 + TypeScript
- Cloudflare Pages
- Global CDN

### **API Gateway**
- Cloudflare Workers
- JavaScript/TypeScript
- Edge Computing

### **Backend Services**
- Spring Boot 3.2
- Java 17
- Cloudflare Containers
- Docker

### **Database**
- Cloudflare D1 (SQLite)
- Global Distribution
- Edge Caching

## 📊 **성능 이점**

### **전 세계 엣지 배포**
- 300+ 데이터센터
- 0ms 콜드 스타트 (Workers)
- 빠른 응답 시간

### **자동 스케일링**
- 트래픽에 따른 자동 확장
- 리소스 최적화
- 비용 효율성

### **고가용성**
- 99.9% 업타임
- 자동 장애 복구
- DDoS 보호

## 🛠️ **구현 단계**

### **1단계: Container 준비**
```bash
# Spring Boot 애플리케이션 컨테이너화
docker build -t macchain-backend .
```

### **2단계: Cloudflare Containers 배포**
```bash
# Container 배포
wrangler containers deploy macchain-backend
```

### **3단계: Workers 연동**
```javascript
// Workers에서 Container 호출
const response = await fetch('https://macchain-backend.containers.dev/api/...');
```

### **4단계: 라우팅 설정**
```javascript
// API Gateway 라우팅
if (path.startsWith('/api/complex/')) {
  return await callContainer(request, env);
} else {
  return await handleDirectly(request, env);
}
```

이 하이브리드 아키텍처로 MacChain의 모든 기능을 Cloudflare 생태계 내에서 최적화할 수 있습니다! 🚀
