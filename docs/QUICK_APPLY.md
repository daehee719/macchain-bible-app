# ⚡ Supabase 스키마 빠른 적용 가이드

## 🎯 지금 바로 적용하기

### 1️⃣ 스키마 SQL 복사 (완료)
✅ 스키마 SQL이 클립보드에 복사되었습니다!

### 2️⃣ Supabase Dashboard에서 실행

**단계별 안내:**

1. **Supabase Dashboard 접속**
   - 링크: https://supabase.com/dashboard
   - 프로젝트: `dazushjgczteromlitve` 선택

2. **SQL Editor 열기**
   - 좌측 사이드바 → **SQL Editor** 클릭
   - 상단 **New query** 버튼 클릭

3. **스키마 SQL 붙여넣기**
   - **Cmd+V** (macOS) 또는 **Ctrl+V** (Windows/Linux)
   - 전체 SQL이 붙여넣어집니다

4. **실행**
   - 우측 상단 **Run** 버튼 클릭
   - 또는 **Cmd+Enter** (macOS) / **Ctrl+Enter** (Windows/Linux)

5. **결과 확인**
   - 성공: "Success. No rows returned" 메시지 확인
   - 에러: 에러 메시지 확인 (대부분 무시해도 됨)

### 3️⃣ 테이블 확인

**Table Editor에서 확인:**
1. 좌측 사이드바 → **Table Editor** 클릭
2. 다음 8개 테이블이 생성되었는지 확인:
   - ✅ `users`
   - ✅ `user_progress`
   - ✅ `macchain_plan`
   - ✅ `reading_progress`
   - ✅ `user_settings`
   - ✅ `ai_analysis`
   - ✅ `monthly_bible_data`
   - ✅ `user_consents`

## 🔄 스키마 SQL 다시 복사하기

클립보드에 복사되지 않았다면:

```bash
./scripts/copy-schema.sh
```

또는:

```bash
cat backend/supabase/database/schema.sql | pbcopy
```

## ✅ 완료 확인

스키마 적용이 완료되면:
- ✅ 8개 테이블 생성 확인
- ✅ RLS 정책 활성화 확인
- ✅ 트리거 및 함수 생성 확인

다음 단계로 진행할 준비가 되었습니다! 🎉

